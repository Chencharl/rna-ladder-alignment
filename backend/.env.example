# Backend environment variables
# Copy to .env and fill in values before running locally.
# Railway injects $PORT automatically — do not set it here for production.

# Optional: restrict CORS origins (comma-separated URLs).
# Leave empty to allow all origins (default).
# ALLOWED_ORIGINS=https://your-frontend.vercel.app,https://www.yourdomain.com
