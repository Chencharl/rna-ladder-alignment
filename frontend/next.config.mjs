/** @type {import('next').NextConfig} */
const nextConfig = {
  // The frontend calls the FastAPI backend directly from the browser.
  // NEXT_PUBLIC_API_URL is injected at build time by Vercel env vars.
};

export default nextConfig;
